name: Node-CI

on:
  push:
    branches: [ master, develop ]

jobs:
  build:

    runs-on: ubuntu-latest
    if: "contains(github.event.head_commit.message, '[update]')"

    steps:
      - name: Copy code files to container
        uses: actions/checkout@v2

      - name: Install Node.js
        uses: actions/setup-node@v1
        with:
          node-version: '14.x'

      - name: Install npm dependencies
        run: npm install

      - name: Run build task
        run: npm run build

  deploy-prod:
    environment: production
    if: "github.ref == 'refs/heads/master'"
    runs-on: ubuntu-18.04
    needs: [ build ]

    steps:
      - name: Copy code files to container
        uses: actions/checkout@v2

      - name: Environment preparation
        run: |
          mkdir -p ~/.ssh/
          touch ~/.ssh/id_rsa
          chmod -R go= ~/.ssh
          chmod 600 ~/.ssh/id_rsa
          echo "${{ secrets.SERVER_SSH_KEY }}" >> ~/.ssh/id_rsa
          sudo apt-add-repository -y -u ppa:ansible/ansible
          sudo apt-get install -y rsync
          sudo apt-get install -y ansible

      - name: Deploy to Server
        run: |
          chmod 700 ci-scripts
          cd ci-scripts
          ansible-playbook -i "${{ secrets.REMOTE_HOST }}" -e "ansible_user=${{ secrets.REMOTE_USER }} \
            DB_LINK=${{ secrets.DB_LINK }} \
            JWT_ACCESS_SECRET=${{ secrets.JWT_ACCESS_SECRET }} \
            JWT_REFRESH_SECRET=${{ secrets.JWT_REFRESH_SECRET }} \
            SERVER_HOST=${{ secrets.SERVER_HOST }} \
            SERVER_PORT=${{ secrets.SERVER_PORT }} \

            SMTP_HOST=${{ secrets.SMTP_HOST }} \
            SMTP_PORT=${{ secrets.SMTP_PORT }} \
            SMTP_SECURE=${{ secrets.SMTP_SECURE }} \
            SMTP_USER=${{ secrets.SMTP_USER }} \
            SMTP_PASSWORD=${{ secrets.SMTP_PASSWORD }} \
            SMTP_SENDER=${{ secrets.SMTP_SENDER }} \

            DB_HOST=${{ secrets.DB_HOST }} \
            DB_USERNAME=${{ secrets.DB_USERNAME }} \
            DB_PASSWORD=${{ secrets.DB_PASSWORD }} \
            DB_NAME=${{ secrets.DB_NAME }} \

            BASE_URL=${{ secrets.BASE_URL }} \

            DEBUG=${{ secrets.DEBUG }} \
            backend_bin=${{ secrets.backend_bin }} \
            PM2_SERVICE_NAME=${{ secrets.PM2_SERVICE_NAME }} \
            SRC_DIR=${{ github.workspace }}/" backend-update.yml;

  deploy-dev:
    environment: develop

    if: "github.ref == 'refs/heads/develop'"
    runs-on: ubuntu-18.04
    needs: [ build ]

    steps:
      - name: Copy code files to container
        uses: actions/checkout@v2

      - name: Environment preparation
        run: |
          mkdir -p ~/.ssh/
          touch ~/.ssh/id_rsa
          chmod -R go= ~/.ssh
          chmod 600 ~/.ssh/id_rsa
          echo "${{ secrets.SERVER_SSH_KEY }}" >> ~/.ssh/id_rsa
          sudo apt-add-repository -y -u ppa:ansible/ansible
          sudo apt-get install -y rsync
          sudo apt-get install -y ansible

      - name: Deploy to Server
        run: |
          chmod 700 ci-scripts
          cd ci-scripts
          ansible-playbook -i "${{ secrets.REMOTE_HOST }}" -e "ansible_user=${{ secrets.REMOTE_USER }} \
            DB_LINK=${{ secrets.DB_LINK }} \
            JWT_ACCESS_SECRET=${{ secrets.JWT_ACCESS_SECRET }} \
            JWT_REFRESH_SECRET=${{ secrets.JWT_REFRESH_SECRET }} \
            SERVER_HOST=${{ secrets.SERVER_HOST }} \
            SERVER_PORT=${{ secrets.SERVER_PORT }} \

            SMTP_HOST=${{ secrets.SMTP_HOST }} \
            SMTP_PORT=${{ secrets.SMTP_PORT }} \
            SMTP_SECURE=${{ secrets.SMTP_SECURE }} \
            SMTP_USER=${{ secrets.SMTP_USER }} \
            SMTP_PASSWORD=${{ secrets.SMTP_PASSWORD }} \
            SMTP_SENDER=${{ secrets.SMTP_SENDER }} \

            BASE_URL=${{ secrets.BASE_URL }} \

            DB_HOST=${{ secrets.DB_HOST }} \
            DB_USERNAME=${{ secrets.DB_USERNAME }} \
            DB_PASSWORD=${{ secrets.DB_PASSWORD }} \
            DB_NAME=${{ secrets.DB_NAME }} \

            FACEBOOK_CLIENT_ID=${{ secrets.FACEBOOK_CLIENT_ID }} \
            FACEBOOK_SECRET_KEY=${{ secrets.FACEBOOK_SECRET_KEY }} \
            FACEBOOK_COOKIE_PASSWORD=${{ secrets.FACEBOOK_COOKIE_PASSWORD }} \
            GOOGLE_CLIENT_ID=${{ secrets.GOOGLE_CLIENT_ID }} \
            GOOGLE_SECRET_KEY=${{ secrets.GOOGLE_SECRET_KEY }} \
            GOOGLE_COOKIE_PASSWORD=${{ secrets.GOOGLE_COOKIE_PASSWORD }} \
            LINKEDIN_CLIENT_ID=${{ secrets.LINKEDIN_CLIENT_ID }} \
            LINKEDIN_SECRET_KEY=${{ secrets.LINKEDIN_SECRET_KEY }} \
            LINKEDIN_COOKIE_PASSWORD=${{ secrets.LINKEDIN_COOKIE_PASSWORD }} \
            TWITTER_CLIENT_ID=${{ secrets.TWITTER_CLIENT_ID }} \
            TWITTER_SECRET_KEY=${{ secrets.TWITTER_SECRET_KEY }} \
            TWITTER_COOKIE_PASSWORD=${{ secrets.TWITTER_COOKIE_PASSWORD }} \

            TWILIO_ACCOUNT_SID=${{ secrets.TWILIO_ACCOUNT_SID }} \
            TWILIO_AUTH_TOKEN=${{ secrets.TWILIO_AUTH_TOKEN }} \
            TWILIO_PHONE_NUMBER_SENDER=${{ secrets.TWILIO_PHONE_NUMBER_SENDER }} \

            CDN_ACCESS_KEY_ID=${{ secrets.CDN_ACCESS_KEY_ID }} \
            CDN_SECRET_ACCESS_KEY=${{ secrets.CDN_SECRET_ACCESS_KEY }} \
            CDN_END_POINT=${{ secrets.CDN_END_POINT }} \
            CDN_BUCKET=${{ secrets.CDN_BUCKET }} \
            CDN_PUB_END_POINT=${{ secrets.CDN_PUB_END_POINT }} \
            CDN_EXPIRES_IN=${{ secrets.CDN_EXPIRES_IN }} \

            SUMSUB_APP_TOKEN=${{ secrets.SUMSUB_APP_TOKEN }} \
            SUMSUB_SECRET_KEY=${{ secrets.SUMSUB_SECRET_KEY }} \
            SUMSUB_BASE_URL=${{ secrets.SUMSUB_BASE_URL }} \
            SUMSUB_ACCESS_TOKEN_TTL=${{ secrets.SUMSUB_ACCESS_TOKEN_TTL }} \

            TOKEN_WQT_BSC_NETWORK_ADDRESS={{ secrets.TOKEN_WQT_BSC_NETWORK_ADDRESS }} \
            TOKEN_WQT_BSC_NETWORK_DECIMAL={{ secrets.TOKEN_WQT_BSC_NETWORK_DECIMAL }} \
            TOKEN_WQT_BSC_NETWORK_SYMBOL={{ secrets.TOKEN_WQT_BSC_NETWORK_SYMBOL }} \
            TOKEN_WQT_BSC_NETWORK_NAME={{ secrets.TOKEN_WQT_BSC_NETWORK_NAME }} \
            TOKEN_WQT_BSC_NETWORK_AMOUNT_MAX={{ secrets.TOKEN_WQT_BSC_NETWORK_AMOUNT_MAX }} \

            TOKEN_WQT_ETHEREUM_NETWORK_ADDRESS={{ secrets.TOKEN_WQT_ETHEREUM_NETWORK_ADDRESS }} \
            TOKEN_WQT_ETHEREUM_NETWORK_DECIMAL={{ secrets.TOKEN_WQT_ETHEREUM_NETWORK_DECIMAL }} \
            TOKEN_WQT_ETHEREUM_NETWORK_SYMBOL={{ secrets.TOKEN_WQT_ETHEREUM_NETWORK_SYMBOL }} \
            TOKEN_WQT_ETHEREUM_NETWORK_NAME={{ secrets.TOKEN_WQT_ETHEREUM_NETWORK_NAME }} \
            TOKEN_WQT_ETHEREUM_NETWORK_AMOUNT_MAX={{ secrets.TOKEN_WQT_ETHEREUM_NETWORK_AMOUNT_MAX }} \

            TOKEN_WETH_ADDRESS={{ secrets.TOKEN_WETH_ADDRESS }} \
            TOKEN_WETH_DECIMAL={{ secrets.TOKEN_WETH_DECIMAL }} \
            TOKEN_WETH_SYMBOL={{ secrets.TOKEN_WETH_SYMBOL }} \
            TOKEN_WETH_NAME={{ secrets.TOKEN_WETH_NAME }} \
            TOKEN_WETH_AMOUNT_MAX={{ secrets.TOKEN_WETH_AMOUNT_MAX }} \

            TOKEN_WBNB_ADDRESS={{ secrets.TOKEN_WBNB_ADDRESS }} \
            TOKEN_WBNB_DECIMAL={{ secrets.TOKEN_WBNB_DECIMAL }} \
            TOKEN_WBNB_SYMBOL={{ secrets.TOKEN_WBNB_SYMBOL }} \
            TOKEN_WBNB_NAME={{ secrets.TOKEN_WBNB_NAME }} \
            TOKEN_WBNB_AMOUNT_MAX={{ secrets.TOKEN_WBNB_AMOUNT_MAX }} \

            WQT_BRIDGE_CONTRACT_RINKEBY=${{ secrets.WQT_BRIDGE_CONTRACT_RINKEBY }} \
            WQT_BRIDGE_CONTRACT_BSCTESTNET=${{ secrets.WQT_BRIDGE_CONTRACT_BSCTESTNET }} \
            WQT_PROVIDER_RINKEBY=${{ secrets.WQT_PROVIDER_RINKEBY }} \
            WQT_PROVIDER_BSCTESTNET=${{ secrets.WQT_PROVIDER_BSCTESTNET }} \
            WQT_PRIVATE_KEY=${{ secrets.WQT_PRIVATE_KEY }} \

            DEBUG=${{ secrets.DEBUG }} \
            backend_bin=${{ secrets.backend_bin }} \
            PM2_SERVICE_NAME=${{ secrets.PM2_SERVICE_NAME }} \
            SRC_DIR=${{ github.workspace }}/" backend-update.yml;
